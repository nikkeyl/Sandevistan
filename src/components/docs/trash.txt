авто fonts.js

авто спрайты

сделать споллеры на details и summary

Архитектура {
    linters.js

    validators.js
}

Проводить чистку Sandevistan, rrc, rrs, docs {
    после завершения активной фазы {
        постепенно находить и исправлять ошибки
        постепенно обновлять код
        постепенно чистить код
        постепенно добавлять новый функционал
        постепенно добавлять новые шрифты
        постепенно добавлять новые сниппеты
        постепенно добавлять новые компоненты в rrc {
            продумать быструю имплементацию в Sandevistan
        }
    }
}

-------------------------------------------

Удалить глобальный обьект

Исправить круговую зависимость

когда поддержка media queries будет 90 - 95% перейти на @media (width >= rem($tablet))

Перенести import webPackConfig в plugins.js

при импорте lightgallery scss purge css выдает ошибку

классы .webp и .no-webp разъединяются и .webp идет после @media (исправить)

fonts.js ускорить и переделать
fonts.js не может работать со шрифтами в папках

Обновить gulp svg sprite

Узнавать автоматически названия файла или функции в errorHandler.js

Разобраться в imagemin

Перенести все вспомогательные конфиги в низ



переписать adaptive-value mixin чтобы при stylelint'инге он не ломался

Favicon работает через раз {
    Png favicon не работает в dev режиме

    Все дело в https, localhost и certs

    Добавить ?v=1
}

eslint нет отступов снизу между блоками кода

stylelint после форматирования ($mobile ) остается ($mobile ) а на ($mobile)


Добавить валидатор для css

Проверка битых ссылок

https://www.npmjs.com/package/gulp-base64-inline

esbuild

Убрать оставшуюся лапшу из package.json

Отключить devtool и sourcemap и посмотреть на реальном проекте

Переделать built и root на что нибудь другое

Подключить --trace-deprecation

Обновить зависимости с помощью overrides

Сборка должна быть: быстрой, удобной, минималистичной.

Pug ?

Перенести библиотеки из npm в libs js

Typescript ?

Крит. css

Автоматическая верстка по ftp | либо отключить

Uncss-webpack-plugin

Удаление неиспользуемого js

Webpack не может работать со вложенными html

Если параллельно задаче dev запустить build то страница будет часто и быстро перезагружаться

Сделать автоформатирование в vscode

Webpack иногда зависает и нужно перезапускать {
    Если появиться ошибка снова записать её

    Либо дело в пк
}

Webpack и html validator не работают без интернета

Починить webpack file include чтобы он не останавливал сервер при не правильном пути к файлу

devserver при изменениях в js полностью перезагружает страницу

Webpack dev server медленно работает

Долго загружает страницу

Ускорить сборку

Добавить подключение локальных шрифтов в head.htm а не в fonts.scss